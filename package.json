{
  "name": "appcreator",
  "version": "1.0.0",
  "description": "Package used to setup Salesforce Connected App used for integration with RedHat Fuse middleware.",
  "main": "js/index.js",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.1.0",
    "eslint-config-google": "^0.8.0",
    "eslint-plugin-header": "^1.0.0",
    "eslint-plugin-jest": "^20.0.3",
    "jest": "^20.0.4",
    "jest-fetch-mock": "^1.1.1",
    "webpack": "^3.0.0"
  },
  "scripts": {
    "build": "yarn test && webpack",
    "lint": "yarn run eslint js",
    "test": "yarn lint && yarn test-only",
    "test-only": "jest --coverage --verbose"
  }
}
